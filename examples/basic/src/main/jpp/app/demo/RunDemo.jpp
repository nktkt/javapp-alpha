package app.demo;

public class RunDemo {
    public static void main(String[] args) {
        String name = args.length > 0 ? args[0] : null;
        String fallback = "Java++";
        System.out.println("Hello, {name ?? fallback}");
    }
}
